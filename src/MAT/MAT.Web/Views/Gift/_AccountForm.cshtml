@model GiftRecipientModel

<fieldset class="well" data-bind="fadeVisible: reader.continue()">
    <legend class="low">2 Your account information</legend>
    @Html.EditorFor(m=>m.AccountInfo)
</fieldset>
