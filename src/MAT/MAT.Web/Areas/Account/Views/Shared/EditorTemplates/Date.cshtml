@model DateTime?

@{
    DateTime dt = DateTime.Now;
    if (Model != null)
    {
        dt = (DateTime) Model;
    }
    @Html.TextBox("", String.Format("{0:d}", dt.ToShortDateString()), new {@class = "datefield input-small", type = "date"})
}